import { CandidatePosition } from './candidate-position.model';

describe('CandidatePosition', () => {
  it('should create an instance', () => {
    expect(new CandidatePosition()).toBeTruthy();
  });
});
